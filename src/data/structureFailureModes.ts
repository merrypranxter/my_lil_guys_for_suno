import { CompositionEngine } from '../types';

function failure(id: string, name: string, subtitle: string, rule: string, shortExplanation: string, tags: string[]): CompositionEngine {
  return { id: 'failure-' + id, domain: 'structure', dimension: 'failureMode', name, subtitle, rule, shortExplanation, tags, accentColor: '#ff5fd7' };
}

export const FAILURE_MODE_ENGINES: CompositionEngine[] = [
  failure('forgetting','FORGETTING','Active rules or facts disappear from working state','At defined stress points remove one active fact, motif, instruction, or relationship from performer memory. The piece must continue from the damaged state instead of quietly restoring what was lost.','Failure propagates through missing remembered structure.',['forgetting','memory','failure']),
  failure('overheating','OVERHEATING','Complexity raises temperature until systems throttle','Track a heat variable that rises with density, loudness, speed, or processing. Crossing thresholds must force throttling, shutdown, simplification, or cooldown sections.','Excess activity causes thermal throttling of the composition.',['heat','throttle','failure']),
  failure('mistranslation','MISTRANSLATION','Converted information changes meaning','Whenever material crosses a language, notation, signal, or representational boundary, introduce a systematic conversion error. The incorrect output becomes real input for later layers.','Meaning mutates during transfer and the mistake propagates.',['translation','error','mutation']),
  failure('buffer-overflow','BUFFER OVERFLOW','Incoming events exceed storage capacity','Give one subsystem a finite queue. If events arrive faster than they are processed, excess items must be dropped, overwritten, duplicated, or spill into another channel according to a defined rule.','Too much incoming material breaks the queue.',['buffer','overflow','queue']),
  failure('tuning-drift','TUNING DRIFT','Reference pitch slowly escapes alignment','Let one voice, instrument, or whole pitch frame drift away from the intended tuning reference. The drift must accumulate measurably until retuning, collapse, or a new equilibrium occurs.','Pitch failure grows gradually rather than arriving as random wrong notes.',['tuning','drift','failure']),
  failure('performer-replacement','PERFORMER REPLACEMENT','A failed role is inherited by a new agent','When a performer can no longer execute their role, replace them with another voice/source that must inherit the current obligations but not necessarily the same capabilities.','Failure transfers responsibility to an imperfect replacement.',['replacement','role','continuity']),
  failure('resource-exhaustion','RESOURCE EXHAUSTION','A finite budget reaches zero','When the active economy/resource pool is depleted, disable the operations that depended on it. Do not invent free replenishment unless another engine explicitly provides recovery.','The system stops doing what it can no longer afford.',['resource','depletion','failure']),
  failure('desynchronization','DESYNCHRONIZATION','Parts lose their shared timing reference','Allow formerly synchronized layers to diverge in phase, tempo, event order, or cue timing. Recovery must require a real re-synchronization event rather than an unexplained snap-back.','The ensemble stops agreeing about when now is.',['sync','timing','failure']),
  failure('identity-collision','IDENTITY COLLISION','Two roles or voices claim the same identity','When two active agents are assigned the same name, role, motif, or ownership token, force conflict over priority, memory, authority, or phrase ownership until a resolution rule separates them.','Duplicate identity creates jurisdictional conflict.',['identity','collision','failure']),
  failure('corrupted-memory','CORRUPTED MEMORY','Stored material returns altered','When recalling a previous motif, rule, fact, or event, mutate one property while preserving enough structure to be recognized as a corrupted recall rather than a new invention.','Memory retrieval itself damages what was stored.',['memory','corruption','recall']),
  failure('bureaucratic-deadlock','BUREAUCRATIC DEADLOCK','Required approvals block one another','Create at least two procedural dependencies that each require authorization from the other or from an unavailable authority. Progress halts until a rule is bypassed, authority changes, or the process fails openly.','The system becomes unable to proceed because its own permissions conflict.',['bureaucracy','deadlock','approval']),
  failure('feedback-runaway','FEEDBACK RUNAWAY','One response amplifies itself each cycle','Choose one variable whose output feeds back as stronger input on the next pass. Continue until clipping, saturation, resource depletion, authority intervention, or another explicit limiter stops it.','A self-reinforcing loop grows beyond normal control.',['feedback','runaway','growth']),
  failure('recursive-error','ERROR CREATES ERROR','Every failure increases probability of another','When a failure occurs, add one new vulnerability, ambiguity, delay, or rule conflict that makes subsequent failure more likely or more severe.','Breakage creates conditions for further breakage.',['error','cascade','failure']),
  failure('cascading-deletion','CASCADING DELETION','Removing one element invalidates dependents','Define dependencies among parts. When one element disappears, any part that relies exclusively on it must also disappear or change function, potentially causing a chain reaction.','A single deletion can collapse a whole dependency tree.',['deletion','cascade','dependency']),
  failure('stuck-state','STUCK STATE','The system cannot leave its current mode','At a trigger point freeze one subsystem in its current setting, chord, loop, word, role, or state. Other systems must work around it until a specific escape condition is met.','One subsystem jams while the rest keeps moving.',['stuck','state','jam']),
  failure('false-success','FALSE SUCCESS SIGNAL','The system declares completion too early','Emit a success/completion state before the real objective has been achieved. Downstream layers must act on the false completion until contradictory evidence forces reopening.','A premature success message creates downstream mistakes.',['status','false-positive','failure']),
  failure('false-alarm','FALSE ALARM','The system enters emergency mode without cause','Trigger a high-priority response despite no underlying failure. The emergency behavior itself must consume resources, interrupt work, or cause secondary consequences.','The response to a nonexistent problem becomes the real problem.',['alarm','false-positive','failure']),
  failure('silent-failure','SILENT FAILURE','A subsystem stops working without announcing it','Disable one mechanism while leaving status indicators apparently normal. The failure is discoverable only through missing consequences, tests, or indirect evidence.','Something breaks quietly and must be inferred from absence.',['silent','hidden','failure']),
  failure('partial-failure','PARTIAL FAILURE','Only one function of a subsystem breaks','Preserve some capabilities while disabling one specific operation. The system must exploit what still works instead of treating the component as entirely dead.','Failure is selective rather than total.',['partial','degradation','failure']),
  failure('intermittent-failure','INTERMITTENT FAILURE','The same subsystem unpredictably works and fails','Alternate functioning and nonfunctioning states according to a bounded rule or probability. Performers must manage uncertainty because success on one attempt does not guarantee the next.','Reliability itself becomes unstable.',['intermittent','reliability','failure']),
  failure('version-mismatch','VERSION MISMATCH','Two components obey different rule editions','Assign two interacting subsystems incompatible versions of the same protocol, manual, format, or vocabulary. Their assumptions must clash until translated or reconciled.','The parts fail because they follow different generations of the same system.',['version','compatibility','failure']),
  failure('overflow-into-neighbor','OVERFLOW INTO WRONG JURISDICTION','Excess material leaks into another subsystem','When one channel exceeds capacity, reroute excess events into a neighboring jurisdiction such as lyrics into rhythm, harmony into noise, or metadata into sung text.','Failure causes cross-jurisdiction contamination.',['overflow','leakage','jurisdiction']),
  failure('control-loop-oscillation','CONTROL LOOP OSCILLATION','Correction repeatedly overshoots','Let a controller react too strongly or too late, causing repeated overshoot around a target value. Tempo, tuning, level, density, or another controlled parameter must oscillate instead of settling.','The repair system becomes the source of instability.',['control','oscillation','overshoot']),
  failure('checkpoint-restart','CHECKPOINT RESTART','Failure restores an earlier saved state','At a terminal error, revert to a designated earlier structural checkpoint. Preserve one scar, warning, or memory token so the restart is not indistinguishable from the original pass.','The system rolls back but carries evidence of the crash.',['restart','checkpoint','rollback']),
  failure('dead-letter','DEAD LETTER','A message can no longer reach any valid recipient','Route one important instruction or lyric through a channel whose destination no longer exists or cannot accept it. The undelivered message must accumulate, bounce, or become archival residue.','Communication fails because there is nowhere valid for the message to go.',['message','delivery','failure']),
];
